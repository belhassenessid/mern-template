>npm init -y
>npm install express cors mongoose dotenv
>(sudo)npm install -g nodemon
configure the server in server.js
configure the mongoDB Atlas connectivity in .env inserting the effective password instead of <password> in the url in this eg the pw is test

create a folder named models to configure the data base models
create a folder named routes to configure the data manipulating operations
>start nodemon server (to test connectivity)