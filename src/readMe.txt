index.js:
don't need neither index.css nor link (and server).

app.js:
don't need neither logo nor link app.css.
>npm start (to open the page in the browser)
>npm install bootstrap
import "bootstrap/dist/css/bootstrap.min.css";
>npm install react-router-dom
import { BrowserRouter as Router, Route} from "react-router-dom";
make the code between <Router>code here</Router> and call components inside them by:
import <component Name> from "./import/fileName.extension"

>npm install axios
create a components folder to contain the components defining files
